version: '3.8'
services:
  db:
    image: mysql:8.0
    container_name: mysql-crud
    restart: always
    environment:
      MYSQL_DATABASE: db_crud_java
      MYSQL_ROOT_PASSWORD: root
    ports:
      - "3306:3306"